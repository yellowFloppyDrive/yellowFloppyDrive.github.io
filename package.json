{
  "name": "yellowfloppydrive",
  "version": "1.0.0",
  "author": "yellowFloppyDrive",
  "scripts": {
    "setup": "cd client && npm ci && cd ../server && npm ci && cd .. && npm ci",
    "dev": "npm run dev-client & npm run dev-server & npm run dev-live-server",
    "dev-client": "cd client && npm run dev",
    "dev-server": "cd server && npm run dev",
    "dev-live-server": "cd public && live-server",
    "build": "cd client && npm run build && cd ../server && npm run build && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
