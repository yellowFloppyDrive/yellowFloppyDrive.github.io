{
  "name": "yellowfloppydrive",
  "version": "1.0.0",
  "author": "yellowFloppyDrive",
  "scripts": {
    "dev": "tsc --watch scripts/*.ts & live-server & wait",
    "build": "tsc scripts/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.6.2"
  }
}
